# Tasks: Portfolio Replication (Next.js → Astro 5 + React 19 + Tailwind)

## Phase 1: Project Setup — Dependencies, Tokens, Fonts, Public Assets

- [x] 1.1 Install all third-party npm packages: `react-animated-cursor`, `react-typed`, `react-parallax-tilt`, `aos`, `react-slick`, `slick-carousel`, `react-modal`, `react-tooltip`, `react-scrollspy-nav`, `react-toastify`, `react-icons`, `@emailjs/browser` — add exact versions to `package.json` and run `npm install`
- [x] 1.2 Inspect `astro.config.mjs` to determine whether Tailwind v3 (`@astrojs/tailwind`) or Tailwind v4 (`@tailwindcss/vite`) is installed; add the correct Tailwind integration and verify `@astrojs/react` is present
- [x] 1.3 Create `src/styles/global.css` — add Poppins `@import` from Google Fonts (weights 300–900), define design token CSS custom properties for light theme (bg `#fff`, text `#7e7e7e`, headings `#1a1a1a`, brand `#34495e`) and dark theme (bg `#111319`, hero bg `#191c26`, sidebar `#34495e`, text `#a9afc3`, headings `#fff`) scoped to `body.theme-light` and `body.theme-dark`, add base resets (box-sizing, margin, font-size 18px body / 16px mobile), and typography scale (h1 45px, h2 36px, h3 30px, h4 24px, h5 20px, h6 16px)
- [x] 1.4 If Tailwind v4: add `@theme` block in `src/styles/global.css` registering `--font-poppins`, color tokens (`--color-brand`, `--color-text`, `--color-heading`), and spacing scale matching CLAUDE.md palette; if Tailwind v3: create `tailwind.config.mjs` with `theme.extend.fontFamily`, `theme.extend.colors`, and `theme.extend.fontSize` entries
- [x] 1.5 Create `public/robots.txt` with content: `User-agent: *\nAllow: /`
- [x] 1.6 Create `public/og-image.jpg` placeholder (1200×630, solid color or placeholder image)
- [x] 1.7 Create `public/cv.pdf` placeholder (empty or 1-page stub) — real file to be supplied before production deploy
- [x] 1.8 Add placeholder images to `src/assets/`: `hero-photo.jpg`, `about-photo.jpg`, `portfolio-mento.jpg`, `portfolio-metro-delivery.jpg`, `portfolio-mingga.jpg` — use any placeholder images during development
- [x] 1.9 Add `public/img/svg/cancel.svg` — close icon SVG used by all modals (simple × mark)
- [x] 1.10 Create `.env.example` at project root with contents: `PUBLIC_EMAILJS_SERVICE_ID=your_service_id`, `PUBLIC_EMAILJS_TEMPLATE_ID=your_template_id`, `PUBLIC_EMAILJS_PUBLIC_KEY=your_public_key`
- [x] 1.11 Update `src/env.d.ts` — add `/// <reference types="astro/client" />` if missing; add `interface ImportMetaEnv` declaring `PUBLIC_EMAILJS_SERVICE_ID`, `PUBLIC_EMAILJS_TEMPLATE_ID`, `PUBLIC_EMAILJS_PUBLIC_KEY` as `string | undefined`

## Phase 2: Content Collections — Zod Schemas + Seed Data

- [x] 2.1 Create `src/content/config.ts` — define `projects` collection with `defineCollection` and Zod schema: `title` (string), `client` (string), `category` (string), `date` (string), `description` (string), `stack` (array of strings), `thumbnail` (image()), `liveUrl` (string URL, optional), `order` (number); define `services` collection with Zod schema: `title` (string), `description` (string), `shortDescription` (string), `icon` (string, optional), `order` (number); export `collections = { projects, services }`; export TypeScript interfaces `Skill`, `Experience`, `Education` matching design contracts
- [x] 2.2 Create `src/content/projects/mento.md` — frontmatter: `title: "Mento"`, `client: "Chile"`, `category: "Fintech Mobile App"`, `date: "2023"`, `stack: ["React Native", "Redux", "Redux Saga", "React Navigation", "Node.js", "Express", "PostgreSQL"]`, `thumbnail: "../../assets/portfolio-mento.jpg"`, `order: 1`; body: description of the fintech app
- [x] 2.3 Create `src/content/projects/metro-delivery.md` — frontmatter: `title: "Metro Delivery"`, `client: "Logistecsa"`, `category: "Logistics Mobile App"`, `date: "2022"`, `stack: ["React Native", "Redux", "Redux Saga", "React Navigation"]`, `thumbnail: "../../assets/portfolio-metro-delivery.jpg"`, `order: 2`; body: description of the logistics app
- [x] 2.4 Create `src/content/projects/mingga.md` — frontmatter: `title: "Mingga"`, `client: "Mingga ONG"`, `category: "Web Application"`, `date: "2021"`, `stack: ["React", "Node.js", "Express", "PostgreSQL"]`, `thumbnail: "../../assets/portfolio-mingga.jpg"`, `order: 3`; body: description of the ONG web app
- [x] 2.5 Create `src/content/services/development-as-service.md` — frontmatter: `title: "Development as a service"`, `shortDescription: "Agile/scrum cycles with client planning sessions"`, `order: 1`; body: full description of agile development service
- [x] 2.6 Create `src/content/services/custom-software.md` — frontmatter: `title: "Custom software"`, `shortDescription: "Tailored solutions with security-first development"`, `order: 2`; body: full description of custom software service
- [x] 2.7 Create `src/content/services/vulnerability-scan.md` — frontmatter: `title: "Vulnerability scan"`, `shortDescription: "Network/infrastructure security assessment"`, `order: 3`; body: full description of vulnerability scan service
- [x] 2.8 Create `src/content/services/mobile-app-development.md` — frontmatter: `title: "Mobile App Development"`, `shortDescription: "Android & iOS, native and hybrid"`, `order: 4`; body: full description of mobile development service
- [x] 2.9 Create `src/content/skills.json` — array of 6 objects `{ "name": string, "percent": number }`: React.js 95, React Native 90, Express 90, Next.js 90, TypeScript 85, PostgreSQL 80
- [x] 2.10 Create `src/content/experience.json` — array of 5 objects `{ "startYear": string, "endYear": string, "role": string, "company": string }`: Mento (2023–Present), Logistecsa-Bentex (2022–Present), Orbis Data (2022–2023), Koscrypt (2019–2022), SDIS (2014–2016)
- [x] 2.11 Create `src/content/education.json` — array of 2 objects `{ "year": string, "degree": string, "institution": string }`: Systems Engineering BSc 2018 (District University of Bogota), Fullstack Bootcamp 2019 (HENRY)

## Phase 3: SEO Layer

- [x] 3.1 Create `src/seo/SEO.astro` — define `Props` interface with `title: string`, `description: string`, `canonicalUrl: string`, `ogImage?: string` (defaults to `"/og-image.jpg"`), `ogType?: "website" | "article"` (defaults to `"website"`); render `<title>`, `<meta name="description">`, `<meta name="robots" content="index, follow">`, `<link rel="canonical">`, and OG tags `og:title`, `og:description`, `og:type`, `og:url`, `og:image` (absolute URL using site base)
- [x] 3.2 Create `src/seo/jsonld.ts` — export `buildPersonSchema(): Record<string, unknown>` returning a `@type: "Person"` JSON-LD object with `name: "Hernán David Figueroa Cárdenas"`, `jobTitle: "Fullstack Developer"`, `email: "davshn@gmail.com"`, `address` (Bogota, Colombia), `sameAs` array of social URLs (Facebook, Twitter, Instagram, GitHub `davshn`, LinkedIn `davshn`); export `buildWebSiteSchema(url: string): Record<string, unknown>` returning `@type: "WebSite"` with `name`, `url`, `description` fields

## Phase 4: BaseLayout + Global Shell

- [x] 4.1 Create `src/layouts/BaseLayout.astro` — define `Props` interface: `title: string`, `description: string`, `canonicalUrl?: string` (defaults to `Astro.url.href`), `ogImage?: string`; import and render `SEO.astro` in `<head>`; inject `buildPersonSchema()` and `buildWebSiteSchema()` results as `<script type="application/ld+json">` tags; import `src/styles/global.css`; add inline `<script>` that reads `localStorage.getItem("theme-color")` synchronously and sets `document.body.className` to the stored value or `"theme-light"` as default (FOWT prevention); add `<script>` that calls `document.addEventListener("DOMContentLoaded", () => AOS.init({ duration: 1200, once: true }))` and imports AOS CSS
- [x] 4.2 Add `CustomCursor.tsx` island to `BaseLayout.astro` with `client:only="react"` directive
- [x] 4.3 Add `ToastContainer` from `react-toastify` to `BaseLayout.astro` as a React island (`client:only="react"`) with props: `position="top-right"`, `autoClose={2000}`, `pauseOnHover`, `draggable`, `closeOnClick`; import `react-toastify/dist/ReactToastify.css`
- [x] 4.4 Add `<slot />` in the `<body>` of `BaseLayout.astro` for page content; add `ScrollToTop.tsx` island with `client:visible`; set `<html lang="en">` and add charset and viewport meta tags

## Phase 5: Sidebar Component

- [x] 5.1 Create `src/components/layout/Sidebar.astro` — render a `<nav>` element fixed left, 320px wide (288px at 1200–1500px), hidden off-screen via `translateX(-100%)` below 1199px, with `transition: all 0.5s ease`; include owner name/logo, five nav links to `#home`, `#about`, `#service`, `#portfolio`, `#contact` in order, wrapped in a scrollspy container
- [x] 5.2 Add scrollspy to `Sidebar.astro` — attempt to use `react-scrollspy-nav` as a React island (`client:load`) wrapping the nav links with `scrollTargetIds={["home","about","service","portfolio","contact"]}`, `activeNavClass="active"`, `offset={0}`, `scrollDuration="100"`; if the package fails at install/runtime, implement a `<script>` using `IntersectionObserver` (~30 lines) that adds the `active` class to the nav link corresponding to the section currently in view
- [x] 5.3 Add social icon links to `Sidebar.astro` — render five `<a>` elements with `target="_blank"` and `rel="noopener noreferrer"` using `react-icons` Feather set: `FiFacebook` → `/david.figueroa.184`, `FiTwitter` → `@davshmr`, `FiInstagram` → `@davidfigueroa9055`, `FiGithub` → `davshn`, `FiLinkedin` → `davshn`
- [x] 5.4 Add hamburger button to `Sidebar.astro` — render a `<button>` visible only below 1199px (CSS class or Tailwind responsive utility); add a `<script>` that toggles the class `menu-open` on the sidebar element when the hamburger is clicked
- [x] 5.5 Add `ThemeToggle.tsx` island slot to `Sidebar.astro` with `client:visible`

## Phase 6: UI Primitives

- [x] 6.1 Create `src/components/ui/Button.astro` — define `Props` interface: `label: string`, `href?: string`, `variant?: "primary" | "outline"` (default `"outline"`), `download?: boolean`; render as `<a>` when `href` is provided (with `download` attribute if `download={true}`), else `<button>`; apply Tailwind utility classes for `border border-[#7e7e7e] border-2 rounded-[6px]` with hover state `hover:bg-[#1a1a1a] hover:text-white`
- [x] 6.2 Create `src/components/ui/Badge.astro` — define `Props` interface: `text: string`; render a `<span>` with appropriate design token classes; must render the text `"6+ Years of Experience"` when that prop is passed
- [x] 6.3 Create `src/components/ui/ProgressBar.astro` — define `Props` interface: `label: string`, `percentage: number`; render a label `<span>` and a progress bar container `<div>` with a fill `<div>` whose `style` is `width: {percentage}%`; add `role="progressbar"`, `aria-valuenow={percentage}`, `aria-valuemin="0"`, `aria-valuemax="100"` on the fill element
- [x] 6.4 Create `src/components/ui/Modal.tsx` — define `Props` interface: `isOpen: boolean`, `onClose: () => void`, `className?: string`, `children: React.ReactNode`; render `react-modal`'s `<ReactModal>` with `className={\`custom-modal \${className ?? ""}\`.trim()}`, `overlayClassName="custom-overlay"`, `closeTimeoutMS={500}`, `onRequestClose={onClose}`; call `ReactModal.setAppElement("#__astro_root")` or body on mount
- [x] 6.5 Create `src/components/ui/ThemeToggle.tsx` — define `FC` with no props; initialize state from `document.body.classList.contains("theme-dark")` in a `useEffect`; on click toggle `theme-light`/`theme-dark` on `document.body` and write to `localStorage["theme-color"]`; render `<FaMoon />` from `react-icons/fa` when light theme active, `<FaSun />` when dark theme active
- [x] 6.6 Create `src/components/ui/ScrollToTop.tsx` — define `FC` with no props; listen to `window.scroll` event in `useEffect`; show button when `window.scrollY > 500`; on click call `window.scrollTo({ top: 0, behavior: "smooth" })`; render a positioned `<button>` that is conditionally visible
- [x] 6.7 Create `src/components/layout/CustomCursor.tsx` — import `AnimatedCursor` from `react-animated-cursor` dynamically to avoid SSR; render `<AnimatedCursor innerSize={8} outerSize={44} color="153,153,255" outerAlpha={0.3} innerScale={0.7} outerScale={1.4} />`

## Phase 7: Hero Section

- [x] 7.1 Create `src/components/sections/TypedHero.tsx` — define `FC` with no props; import `ReactTyped` from `react-typed`; render `<ReactTyped strings={["Fullstack Developer", "Web Developer", "Mobile Developer"]} typeSpeed={150} backSpeed={60} backDelay={1} loop showCursor cursorChar="|" />`; ensure no SSR output (island uses `client:load`)
- [x] 7.2 Create `src/components/sections/Hero.astro` — render a section with `id="home"`; import and use `astro:assets` `<Image>` for the hero photo from `src/assets/hero-photo.jpg` with `alt="David Figueroa - Fullstack Developer"` and explicit `width`/`height`; add `data-aos="fade-right"` on the photo column and `data-aos="fade-left"` with `data-aos-duration="1200"` on the text column; render `TypedHero.tsx` island with `client:load`; render `"Fullstack Developer based In Colombia, over 6 years of professional experience."` bio paragraph sourced from a prop or content constant (not hardcoded in template); render five social icon links matching the sidebar (FiFacebook, FiTwitter, FiInstagram, FiGithub, FiLinkedin) from `react-icons/fi` with correct hrefs, `target="_blank"`, `rel="noopener noreferrer"`; render `Button` primitive with `label="Download CV"` `href="/cv.pdf"` `download={true}`

## Phase 8: About Section

- [x] 8.1 Create `src/components/sections/AboutModal.tsx` — define `Props` interface: `isOpen: boolean`, `onClose: () => void`, `experience: Experience[]`, `education: Education[]`; render `Modal` primitive with `className="about-popup-wrapper"`; render four sub-sections as tabs or sequential layout: (a) Personal Info — Name, Location, Phone, Email, Languages, Discord, Freelance from hardcoded constants matching CLAUDE.md; (b) Achievements — `06 years experience`, `37 completed projects`, `21 happy customers`; (c) Experience — iterate `experience` prop; (d) Education — iterate `education` prop; render close button using `/img/svg/cancel.svg` that calls `onClose`
- [x] 8.2 Create `src/components/sections/About.astro` — import `getCollection` from `astro:content`; import `skills.json` and `experience.json` and `education.json`; query skills array; render section with `id="about"`; render left column with `data-aos="fade-right"` `data-aos-duration="1200"` containing bio text and `ProgressBar` primitive for each skill (iterate over skills JSON); render right column with `data-aos="fade-left"` `data-aos-duration="1200"` containing `astro:assets` `<Image>` for about photo with `alt="David Figueroa"` and `Badge` primitive with `text="6+ Years of Experience"`; render `AboutModal.tsx` island with `client:visible` passing `experience` and `education` data as props; render "See More" button that sets modal open state — wire button click to modal open via a React island wrapper or client-side script

## Phase 9: Service Section

- [x] 9.1 Create `src/components/sections/ServiceCards.tsx` — define `ServiceItem` interface: `id: string`, `title: string`, `icon: string`, `shortDescription: string`, `body: string`; define `Props` interface: `services: ServiceItem[]`; render four `react-parallax-tilt` wrapped cards (default tilt settings); track `activeService: string | null` state; on card click set `activeService` to service id; render `Modal` primitive for the selected service with `className="custom-modal"` and close button using `/img/svg/cancel.svg`; apply `data-aos="fade-up"` `data-aos-duration="1200"` with `data-aos-delay` of 100, 200, 300, 400 on the four cards respectively
- [x] 9.2 Create `src/components/sections/Service.astro` — import `getCollection` from `astro:content`; query `services` collection sorted by `order`; render section with `id="service"`; pass collection data (id, title, shortDescription, body) as props to `ServiceCards.tsx` island with `client:visible`

## Phase 10: Portfolio Section

- [x] 10.1 Create `src/components/sections/PortfolioCarousel.tsx` — define `Project` interface: `id: string`, `title: string`, `client: string`, `category: string`, `date: string`, `liveUrl?: string`, `stack: string[]`, `thumbnail: string`, `body: string`; define `Props: { projects: Project[] }`; configure `react-slick` `Slider` with responsive settings: default `{ slidesToShow: 3, speed: 800, infinite: true, draggable: false }`, breakpoint at 575px `{ slidesToShow: 1, speed: 300, draggable: true, dots: true, arrows: false }`; render each project as a slide with thumbnail `<img>` wrapped in `react-tooltip` data attributes (`data-tooltip-id`, `data-tooltip-content={project.title}`); render `<Tooltip id="..." place="bottom" variant="light" float />` per slide; track `activeProject: string | null` state; on thumbnail/card click open `Modal` primitive for that project displaying title, description, client, category, date, stack, and `liveUrl` link (only render link when `liveUrl` is defined); render close button using `/img/svg/cancel.svg`; add `data-aos="fade-up"` `data-aos-duration="1200"` on the carousel container wrapper
- [x] 10.2 Create `src/components/sections/Portfolio.astro` — import `getCollection` from `astro:content`; import `getImage` or use `getEntry` with image; query `projects` collection sorted by `order`; serialize each project's thumbnail to a string URL using `getImage`; render section with `id="portfolio"`; pass serialized project data array as props to `PortfolioCarousel.tsx` island with `client:visible`; import `slick-carousel/slick/slick.css` and `slick-carousel/slick/slick-theme.css`

## Phase 11: Contact Section

- [x] 11.1 Create `src/components/sections/ContactForm.tsx` — define `FC` with no props; use `useRef<HTMLFormElement>` for form reference; read `import.meta.env.PUBLIC_EMAILJS_SERVICE_ID`, `PUBLIC_EMAILJS_TEMPLATE_ID`, `PUBLIC_EMAILJS_PUBLIC_KEY`; if any env var is missing, disable submit and show a warning; track `isSubmitting: boolean` state; on submit: set `isSubmitting = true`, call `emailjs.sendForm(serviceId, templateId, formRef.current!, publicKey)`; on success call `toast("Message Sent Successfully!")` and reset form; on failure call `toast("Ops Message Not Sent!")`; always set `isSubmitting = false`; render form with `name`, `email`, `message` inputs all marked `required`; disable submit button when `isSubmitting` is true
- [x] 11.2 Create `src/components/sections/Contact.astro` — render section with `id="contact"`; render three address card `<div>`s with `data-aos="fade-up"` `data-aos-duration="1200"` and staggered `data-aos-delay` (100, 200, 300): phone `+57 3106961637`, email `davshn@gmail.com`, location `Galan, Bogota, Colombia`; render `ContactForm.tsx` island with `client:visible`; render Google Maps `<iframe>` with `src` referencing coordinates `4.6482975, -74.107807` (use Google Maps embed URL format: `https://maps.google.com/maps?q=4.6482975,-74.107807&...`), explicit `width="100%"` and `height="400"` to prevent CLS

## Phase 12: Custom Cursor Island (Finalize)

- [x] 12.1 Verify `src/components/layout/CustomCursor.tsx` (created in 6.7) is correctly mounted in `BaseLayout.astro` with `client:only="react"` (not `client:load`) to avoid any SSR rendering; confirm `npm run build` completes with zero errors for the cursor island — scenario: SSR must not throw for cursor

## Phase 13: Index Page Wiring

- [x] 13.1 Modify `src/pages/index.astro` — replace the existing placeholder content with `<BaseLayout title="David Figueroa | Fullstack Developer" description="Personal portfolio of David Figueroa — Fullstack Developer based in Bogota, Colombia with 6+ years of experience." canonicalUrl="https://yourdomain.com">` wrapping all sections; import and render in this exact order inside the layout slot: `Sidebar.astro`, `Hero.astro` (id=home), `About.astro` (id=about), `Service.astro` (id=service), `Portfolio.astro` (id=portfolio), `Contact.astro` (id=contact); verify each section component's wrapper element has the corresponding `id` attribute

## Phase 14: Final QA Pass

- [x] 14.1 Run `npm run build` and confirm zero TypeScript strict-mode errors, zero Astro build errors, and zero content collection Zod validation errors — fix any type errors found
- [x] 14.2 Run `npm run lint` (ESLint + Prettier) and confirm zero lint errors — fix any lint issues found
- [x] 14.3 Run `npm run preview` and manually verify in browser: (a) AOS animations trigger on scroll for all sections; (b) scroll-to-top button appears after scrolling 500px and clicking it scrolls smoothly to top; (c) theme toggle persists between page reloads via `localStorage["theme-color"]`; (d) sidebar scrollspy `active` class updates correctly as sections enter viewport
- [x] 14.4 Manually verify responsive behavior at viewport width < 1199px: (a) sidebar is hidden off-screen; (b) hamburger button is visible and slides sidebar in/out; (c) portfolio carousel shows one item; (d) font-size is 16px on body
- [x] 14.5 Manually verify SEO output using browser View Source: `<title>` is not the Astro default, `<meta name="description">` is present, `<link rel="canonical">` is present, all five OG tags are present, `<script type="application/ld+json">` contains valid Person schema with `name: "Hernán David Figueroa Cárdenas"`
- [x] 14.6 Manually verify all modal interactions: About "See More" modal opens/closes (close button and overlay click), all four Service modals open correct content, all three Portfolio project modals open correct content with live link present only for Mento (if liveUrl defined)
- [x] 14.7 Manually verify contact form: form shows validation errors on empty/invalid submit; with valid EmailJS env vars filled in `.env`, form submits, shows success toast, and resets fields; disconnect network and verify error toast appears without uncaught JS exception
