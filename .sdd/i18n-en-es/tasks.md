# Tasks: Bilingual i18n Support (English / Spanish)

## Phase 1: Foundation — i18n Config, Utils, and Message Files

- [x] 1.1 Add `i18n` config block to `astro.config.mjs` — set `defaultLocale: "en"`, `locales: ["en", "es"]`, `routing.prefixDefaultLocale: true`, `routing.redirectToDefaultLocale: true`
- [x] 1.2 Create `src/i18n/utils.ts` — export `Locale` type union (`"en" | "es"`), all `Messages` sub-interfaces (`NavMessages`, `HeroMessages`, `AboutMessages`, `ServiceMessages`, `PortfolioMessages`, `ContactMessages`, `CommonMessages`), the root `Messages` interface, `getLang(url: URL): Locale`, `getTranslation(lang: Locale): Messages`, `AlternateLink` interface, and `getAlternates(siteBase: string, path?: string): AlternateLink[]`
- [x] 1.3 Create `src/i18n/messages/en.json` — complete English UI strings following the namespaced key schema defined in design.md: `nav`, `hero` (including `typedStrings` array and `seo` sub-key with `title`/`description`), `about` (including all `modal.*` sub-keys), `service`, `portfolio`, `contact` (including all `form.*` sub-keys), and `common` (including `themeToggle`, `langToggle`, and `closeModal`); also add `toast.success` and `toast.error` keys for `ContactForm.tsx`
- [x] 1.4 Create `src/i18n/messages/es.json` — Spanish UI strings mirroring the exact same key structure as `en.json` with all values in Spanish; typed role strings in `hero.typedStrings` must be Spanish equivalents; no English strings as values

## Phase 2: Content Collections Migration

- [x] 2.1 Update `src/content/config.ts` — add `lang: z.enum(["en", "es"])` as a required field to both the `projects` collection schema and the `services` collection schema
- [x] 2.2 Migrate existing project files to `src/content/projects/en/` — move `mento.md`, `metrologistica.md`, `foundry-vtt.md`, `cyberquest.md`, `forgeai.md` into the `en/` subdirectory and add `lang: "en"` to each file's frontmatter; do NOT delete originals until `en/` copies are confirmed
- [x] 2.3 Create Spanish project translations in `src/content/projects/es/` — create `mento.md`, `metrologistica.md`, `foundry-vtt.md`, `cyberquest.md`, `forgeai.md` with Spanish `title`, `description`, and `category` values; keep `stack`, `thumbnail`, `liveUrl`, `order`, and `client` values unchanged; add `lang: "es"` to each
- [x] 2.4 Migrate existing service files to `src/content/services/en/` — move `development-as-service.md`, `custom-software.md`, `vulnerability-scan.md`, `mobile-app-development.md` into the `en/` subdirectory and add `lang: "en"` to each file's frontmatter
- [x] 2.5 Create Spanish service translations in `src/content/services/es/` — create `development-as-service.md`, `custom-software.md`, `vulnerability-scan.md`, `mobile-app-development.md` with Spanish `title`, `shortDescription`, and body description; keep `icon`, `order` unchanged; add `lang: "es"` to each
- [x] 2.6 Delete the original flat content files — remove `src/content/projects/*.md` (flat) and `src/content/services/*.md` (flat) now that `en/` subdirectory counterparts exist; do this as a single operation to avoid duplicate Astro content IDs
- [x] 2.7 Create `src/content/skills.es.json` — Spanish skill name translations with the same array structure as `skills.json`; percentage values must be identical to the English originals
- [x] 2.8 Create `src/content/experience.es.json` — Spanish role title and company name translations with the same array structure as `experience.json`
- [x] 2.9 Create `src/content/education.es.json` — Spanish degree title and institution name translations with the same array structure as `education.json`

## Phase 3: Routing and Layout

- [x] 3.1 Create `src/pages/[lang]/index.astro` — implement `getStaticPaths()` returning `[{ params: { lang: "en" } }, { params: { lang: "es" } }]`; extract `lang` from `Astro.params` cast to `{ lang: Locale }`; call `getTranslation(lang)` and `getAlternates()`; pass `lang`, `t`, and `alternates` to `BaseLayout` and all section components
- [x] 3.2 Replace `src/pages/index.astro` — remove (or redirect) the existing flat index page; the Astro i18n `redirectToDefaultLocale: true` config handles `/` → `/en` so the file can be deleted or replaced with a minimal redirect stub if the config alone is insufficient for the static adapter
- [x] 3.3 Update `src/layouts/BaseLayout.astro` — add `lang: Locale` (required) and `alternates?: AlternateLink[]` to the `Props` interface; change `<html lang="en">` to `<html lang={lang}>`; pass `lang` and `alternates` down to `<SEO>`; add a new `is:inline` locale no-flash script in `<head>` that reads `localStorage.getItem("portfolio-lang")` and calls `window.location.replace()` if the stored locale differs from the current URL segment (matching the existing theme-color no-flash pattern); do NOT modify the existing theme script
- [x] 3.4 Update `src/seo/SEO.astro` — add `lang?: Locale` and `alternates?: AlternateLink[]` to the `Props` interface; emit `{alternates?.map((alt) => <link rel="alternate" hreflang={alt.hreflang} href={alt.href} />)}` after the canonical `<link>`; add `<meta property="og:locale">` and `<meta property="og:locale:alternate">` tags derived from `lang` (en → `en_US`, es → `es_ES`)
- [x] 3.5 Update `src/seo/jsonld.ts` — change `buildPersonSchema()` signature to `buildPersonSchema(lang: Locale)` and `buildWebSiteSchema()` to `buildWebSiteSchema(url: string, lang: Locale)`; add `inLanguage: lang === "es" ? "es-CO" : "en-US"` to each schema; use locale-aware `description` string from message files (import `getTranslation` and use `t.seo.description`)

## Phase 4: Sidebar and Language Toggle

- [x] 4.1 Create `src/components/ui/LangToggle.astro` — accept `lang: Locale` prop; render two `<a>` tags pointing to `/en` and `/es` with `class:list={['lang-btn', { active: lang === 'en' }]}` and `class:list={['lang-btn', { active: lang === 'es' }]}`; add `is:inline` no-flash script that reads `localStorage["portfolio-lang"]` and redirects if stored locale differs from current URL segment and stored value is a valid locale; add a module `<script>` that attaches click listeners to `.lang-btn` elements and calls `localStorage.setItem("portfolio-lang", chosen)` on each click
- [x] 4.2 Update `src/components/layout/Sidebar.astro` — add `lang: Locale` to the `Props` interface; replace hardcoded nav label strings with `t.nav.*` values from `getTranslation(lang)`; import and render `<LangToggle lang={lang} />` below the `ThemeToggle` component; replace hardcoded theme toggle labels with `t.common.themeToggle.*` values

## Phase 5: Section Components

- [x] 5.1 Update `src/components/sections/Hero.astro` — add `lang: Locale` to `Props`; call `getTranslation(lang)` to get `t`; replace hardcoded greeting, bio text, and CV button label with `t.hero.*` values; pass `strings={t.hero.typedStrings}` to `<TypedHero>`
- [x] 5.2 Update `src/components/sections/TypedHero.tsx` — replace hardcoded `strings` array with a `strings: string[]` prop in the component's `Props` interface; remove the hardcoded `["Fullstack Developer", "Web Developer", "Mobile Developer"]` constant; the component renders whatever strings are passed in
- [x] 5.3 Update `src/components/sections/About.astro` — add `lang: Locale` to `Props`; call `getTranslation(lang)`; replace hardcoded section label, title, skills heading, experience badge, and "See More" button text with `t.about.*` values; load skills/experience/education data conditionally: `lang === "es" ? skillsEs : skillsEn` (import both JSON files statically; use a try/catch or existence check to fall back to English if the Spanish file is absent); pass `messages={t.about}` to `<AboutIsland>`
- [x] 5.4 Update `src/components/sections/AboutIsland.tsx` — update `Props` interface to accept `messages: AboutMessages` (replacing any `lang` prop); pass `messages` down to `<AboutModal>`; remove any hardcoded UI strings from this component
- [x] 5.5 Update `src/components/sections/AboutModal.tsx` — update `Props` interface to accept `messages: AboutMessages`; replace all hardcoded tab labels (`"Personal Info"`, `"Achievements"`, `"Experience"`, `"Education"`) and section headings with `messages.modal.tabs.*` and `messages.modal.*.heading` values; replace `"Available"` freelance value with `messages.modal.personal.freelanceValue`
- [x] 5.6 Update `src/components/sections/Service.astro` — add `lang: Locale` to `Props`; filter `getCollection("services")` with `.filter((e) => e.data.lang === lang)`; strip locale prefix from entry IDs before passing to `ServiceCards` via `entry.id.replace(/^(en|es)\//, "")`; pass `messages={t.service}` to `<ServiceCards>`; replace hardcoded section heading/label with `t.service.*` values
- [x] 5.7 Update `src/components/sections/ServiceCards.tsx` — update `Props` interface to accept `messages: ServiceMessages` (replacing any `lang` prop); use `messages.modal.closeAriaLabel` for modal close button aria-label; remove any hardcoded UI strings
- [x] 5.8 Update `src/components/sections/Portfolio.astro` — add `lang: Locale` to `Props`; filter `getCollection("projects")` with `.filter((e) => e.data.lang === lang)`; strip locale prefix from entry IDs via `entry.id.replace(/^(en|es)\//, "")`; pass `messages={t.portfolio}` to `<PortfolioCarousel>`; replace hardcoded section heading/label with `t.portfolio.*` values
- [x] 5.9 Update `src/components/sections/PortfolioCarousel.tsx` — update `Props` interface to accept `messages: PortfolioMessages` (replacing any `lang` prop); use `messages.modal.clientLabel`, `messages.modal.categoryLabel`, `messages.modal.dateLabel`, `messages.modal.liveLinkText`, and `messages.modal.closeAriaLabel` in the project detail modal; remove all hardcoded label strings
- [x] 5.10 Update `src/components/sections/Contact.astro` — add `lang: Locale` to `Props`; call `getTranslation(lang)`; replace hardcoded section heading/label and address card titles (`"Phone"`, `"Email"`, `"Location"`) with `t.contact.*` values; pass `messages={t.contact}` to `<ContactForm>`
- [x] 5.11 Update `src/components/sections/ContactForm.tsx` — update `Props` interface to accept `messages: ContactMessages` (replacing any `lang` prop); use `messages.form.*` for all field labels, placeholders, and submit button text; use `messages.toast.success` and `messages.toast.error` for the `toast()` call arguments (add `toast` sub-keys to `ContactMessages` interface if not present in design — i.e., add `toast: { success: string; error: string }` to `ContactMessages` in `utils.ts` and populate in both JSON files)

## Phase 6: Verification and Build Check

- [x] 6.1 Run `astro build` and confirm the output `dist/` directory contains both `en/index.html` and `es/index.html` with no build errors
- [x] 6.2 Grep `dist/en/index.html` for `lang="en"` on the `<html>` element, three `<link rel="alternate" hreflang>` tags (`en`, `es`, `x-default`), and `<link rel="canonical" href="https://davshn.dev/en">` — all must be present
- [x] 6.3 Grep `dist/es/index.html` for `lang="es"` on the `<html>` element, the same three `hreflang` tags, and `<link rel="canonical" href="https://davshn.dev/es">` — all must be present
- [ ] 6.4 Manually verify the language toggle in a browser: navigate to `/en`, click `ES`, confirm URL changes to `/es` and `localStorage["portfolio-lang"]` is `"es"`, then reload and confirm no redirect back to `/en`
- [ ] 6.5 Manually verify no English text is visible on `/es` — check Hero bio, About section headings, service card titles, portfolio modal labels (Client, Category, Date), contact form labels, and toast messages
- [x] 6.6 Confirm `TypeScript` compilation passes with zero errors (`astro check` or `tsc --noEmit`) — verifies that all `Messages` key accesses are type-safe and no component has a missing `lang` or `messages` prop
